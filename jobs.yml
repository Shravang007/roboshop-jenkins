- name: Create Jenkins Jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - user
      - shipping
      - payment
      - frontend
      - dispatch
  roles:
    - jenkins-jobs

